= simple_form_for BetaRequest.new, remote: true, html: { class: 'form-horizontal'} do |f|
  = f.error_notification
  = f.input :name
  = f.input :email
  = f.input :oops, as: :hidden
  .form-actions
    = f.button :submit, "Sign Up"
