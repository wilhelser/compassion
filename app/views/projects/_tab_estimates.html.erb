<div class="tab-pane" id="tab6">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Date</th>
        <th>Amount</th>
        <th>Description</th>
        <th>Document</th>
      </tr>
    </thead>
    <tbody>
     <% if @estimates.any? %>
      <% @estimates.each do |estimate| %>
        <tr>
          <td><%= estimate.created_at.strftime('%b %d, %Y') %></td>
          <td><%= estimate.amount %></td>
          <td><%= estimate.description %><td>
          <td><a href="<%= estimate.document.url %>" target="_blank">Estimate</a></td>
        </tr>
      <% end %>
    <% end %>
    </tbody>
  </table>
</div>