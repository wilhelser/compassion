#assignments
  %table#adjuster-dashboard-assignments.table.table-hover
    %thead
      %tr
        %th Date
        %th Project
        %th Contractor
        %th Actions
    %tbody
      #assignments-wrap
        = render @assignments if @assignments.any?
= render 'estimates/new_estimate_modal'
= render 'estimates/edit_estimate_modal'
